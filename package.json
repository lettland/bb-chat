{
  "name": "vch",
  "version": "0.1.0",
  "description": "BB in your terminal — a full TUI client for the BB coding-agent server.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "vch": "./bin/vch.js"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "files": [
    "src",
    "bin",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build:binary": "bun build ./src/index.ts --compile --outfile dist/vch",
    "build:binaries": "bash scripts/build-binaries.sh",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun test"
  },
  "dependencies": {
    "@opentui/core": "0.5.11",
    "bb-app": "0.43.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/bun": "latest",
    "typescript": "7.0.2"
  }
}
